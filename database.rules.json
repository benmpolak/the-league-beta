{
  "rules": {
    ".read": false,
    ".write": false,
    "leagues": {
      "$league": {
        ".read": true,
        ".write": "newData.exists() || !data.exists() || data.child('phase').val() === 'setup'"
      }
    }
  }
}
