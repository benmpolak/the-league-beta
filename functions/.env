# PRE-CUTOVER ONLY: main/Pages does not serve data/data.json until auth-v2
# merges, so deployed functions read the feed from the branch directly.
# AT CUTOVER (runbook step 7): DELETE this line and redeploy functions so
# they read https://benmpolak.github.io/the-league (kept fresh by the Action).
DATA_BASE_URL=https://raw.githubusercontent.com/benmpolak/the-league/auth-v2
